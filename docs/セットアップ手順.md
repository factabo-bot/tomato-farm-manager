# セットアップ手順

## 1. スプレッドシートとGASの準備

1. Googleドライブで新しいスプレッドシートを作成する（例：「栽培管理DB」）
2. メニュー「拡張機能」→「Apps Script」を開く
3. デフォルトの `コード.gs` の中身を全部消して、`gas/Code.gs` の内容を貼り付けて保存
4. エディタ上部の関数選択で `setup` を選び、実行ボタン（▷）を押す
   - 初回は権限の承認画面が出るので許可する
   - 実行後、スプレッドシートに6枚のシート（作業記録／農薬散布記録／マスタ_拠点棟／マスタ_作業分類／マスタ_農薬／マスタ_品目）が作られ、マスタに初期データが入る
5. 「デプロイ」→「新しいデプロイ」→ 種類の選択で「ウェブアプリ」を選ぶ
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**
   - 「デプロイ」を押す
6. 発行された `…/exec` で終わるURLをコピーする

## 2. フロントエンドの設定

1. `frontend/config.js` を開き、`GAS_URL` に手順1-6でコピーしたURLを貼る
   ```js
   const CONFIG = {
     GAS_URL: "https://script.google.com/macros/s/XXXXXXXX/exec",
     APP_TOKEN: "",
   };
   ```
2. `GAS_URL` が空のままだとブラウザのlocalStorageだけで動く「お試しモード」になる（動作確認用）

## 3. マスタデータの調整

スプレッドシートの各マスタシートを直接編集して運用する（アプリ側に管理画面は作っていない）。

- **マスタ_拠点棟**：拠点・棟が増えたら行を追加。使わなくなったら「有効フラグ」をFALSEにする（削除しなくてよい）
- **マスタ_作業分類**：作業の種類を増減
- **マスタ_農薬**：使用する農薬が増えたら追加。`10_Tomato/03_知識・ノウハウ/病害虫/農薬・防除メモ.md` を随時反映する
- **マスタ_品目**：栽培する品目・品種を追加

編集後、アプリを開き直す（またはリロード）とマスタが反映される。

**2027年1月から農政センターのハウス1棟を使う場合**：マスタ_拠点棟の「農政センター｜ハウス1棟」行の「有効フラグ」をTRUEに変更する。

## 4. GitHub Pagesでの公開

1. GitHubで新しいリポジトリを作成（例：`tomato-farm-manager`、公開リポジトリ）
2. `frontend/` の中身をリポジトリのルート（またはルート直下の `docs/` フォルダ）にpush
3. リポジトリの Settings → Pages で公開ソースを設定
4. 発行されたURL（`https://<ユーザー名>.github.io/tomato-farm-manager/`）にアクセスできることを確認

## 5. Androidでホーム画面に追加する

1. Android端末のChromeで公開URLを開く
2. メニュー（縦三点） →「ホーム画面に追加」または自動表示されるインストールバナーをタップ
3. ホーム画面のアイコンから開くとPWAとして（アドレスバーなしで）起動する

## 6. スタッフ運用に切り替える場合（将来）

1. Apps Scriptエディタの「プロジェクトの設定」→「スクリプトプロパティ」に `APP_TOKEN` を追加し、適当な文字列を設定
2. `frontend/config.js` の `APP_TOKEN` に同じ値を設定
3. これで、トークンを知らない第三者はURLを知っていても書き込めなくなる

## 動作確認のポイント

- 法定5項目（使用年月日・使用場所・農作物の種類・農薬の種類/名称・希釈倍数or使用量）のいずれかを空欄で農薬記録を送信し、エラーになることを確認する
- Android PWAとPCブラウザの両方から記録・履歴を確認し、同じデータが見えることを確認する（データ共有の確認）
- 機内モードでフォーム送信 → 「保留しました」の表示 → オンラインに戻すと自動送信されることを確認する（送信キューの確認）
